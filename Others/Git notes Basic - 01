 Usernamne config : git config --global user.name "username"
 Email config  : git config --gloabal   user.email "abc@gmail.com"
 
 Show list : 
 git config --list 
 git config user.name
 git config user.email 
 
 clear screen : ctrl + l
 
 
 Creat git Folder :
 
 mkdir notes
 cd notes 
 ls (show file in directory)
 ls -a (show hidden file also)
 git init 
 touch f1.txt 
 start f1.txt 
 git status (Untracked files) 
 
 
Staging :

git add filename 
git add -A (all file in directory and subdirectory)
git add . (only dir)

git add*.js(dir wildcard)
git add**/*.js(dir and subdir)

git restore 
git diff (check diff)




Commit (Stage to Local) : 


git commit -m "message" (stage to local)
git commit -am "message" (stage + commit)
git log (commit history)
git checkout filename (undo)

Uncommit :

git reset --soft HEAD^ (stage)
git reset HEAD^ (working dir)
git reset --hard HEAD^ (delete)


git show commit id 
git log --oneline 
git checkout commit id Or master





 
